sipantichijk@penguin:~/efinity/2026.1/pt/bin$ grep -n "version\|migrate\|2025\|def " ~/efinity/2026.1/pt/bin/design/design_migrator.py | head -30
ls ~/efinity/2026.1/bin/ | head -30
85:def update_migrated_new_param(migration_log, new_obj: PeriDesignGenPinItem, old_obj: PeriDesignGenPinItem):
95:    :param new_obj: Object that was newly created based on the migrated
124:def update_migrated_new_pin(migration_log: MigrationLog, new_obj: PeriDesignGenPinItem,
135:    :param new_obj: Object that was newly created based on the migrated
161:                    f'Existing {old_pin_type} pin is not migrated due to unsupported pin type change')
171:    # In future, will have user facing block version for each type.
218:    def __init__(self):
225:    def set_block_type(self, old_block_type: PeriDesign.BlockType, new_block_type: PeriDesign.BlockType):
236:    def add_log(self, functionality: str, msg: str):
246:    def add_log_data(self, log_data: LogData):
255:    def reg_device_change(self, old_device: str, new_device: str):
265:    def reg_block_change(self, old_block_type: Union[PeriDesign.BlockType, None],
293:    def reg_param_change(self, inst_name: str, msg: str):
303:    def write_header(self, file_obj):
309:        from _version import __version__
312:        file_obj.write("Version: {}\n".format(__version__))
315:    def write_log(self, file_obj):
352:    def __init__(self, new_design: db.PeriDesign, new_block_type: db.PeriDesign.BlockType,
355:        self.new_block_type = new_block_type  #: New block type to migrate to
356:        self.old_block_type = old_block_type  #: Old block type to migrate from
362:    def migrate(self):
369:    def migrate_registry(self):
373:        With data migration, it will create new empty registry and then migrate individual block instance.
377:    def migrate_block(self, new_inst: PeriDesignItem, old_inst: PeriDesignItem):
398:    def __init__(self, new_design: db.PeriDesign, new_block_type: db.PeriDesign.BlockType,
420:    def migrate(self):
433:                self.migrate_registry()
435:    def migrate_registry(self):
452:        # Rebuild and migrate gpio instance
459:                    inst.gpio_def = ""
80-efx-pgm.rules
QtWebEngineProcess
_efx_security_util
app.ini
app_session.ini
dark.qss
dark.xml
dark_base.qss
dbg_wizard
efinity
efinity_pt.sh
efinity_sh.sh
efx_bram_edit
efx_code
efx_export
efx_map
efx_map_options.xml
efx_map_ramest
efx_pgm
efx_pgm_options.xml
efx_pnr
efx_pnr_options.xml
efx_py
efx_run
efx_sta
efx_vdb_util
enf_proj.xsd
enf_topt.xsd
export_ooc_restrict_ip.txt
hw_tools.json
sipantichijk@penguin:~/efinity/2026.1/pt/bin$ 